{$PUBLIC_HOST} {
	encode gzip zstd

	basic_auth {
		{$BASIC_AUTH_USERNAME} {$BASIC_AUTH_PASSWORD_HASH}
	}

	@backend path /api/* /health
	reverse_proxy @backend autods-server:8000

	reverse_proxy autods-frontend:3000
}
